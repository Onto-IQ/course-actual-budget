---
title: "[Lab 05] Spending summary card (FE + tests) with Playwright evidence"
labels: lab-05, frontend, multi-agent
lab: 05
---

## Context

Claude Agent Teams or parallel subagents + worktrees. QA uses Playwright MCP.

## Task

Implement `course/src/spendingSummary.ts` and a minimal HTML fixture `course/fixtures/spending-summary.html` that renders top categories from sample JSON.

Add `course/tests/spendingSummary.test.ts`.

## Acceptance criteria

- [ ] Unit tests green.
- [ ] Playwright MCP (or `npx playwright`) screenshot of the fixture attached to the PR.
- [ ] PR shows more than one agent/teammate contribution (commits or PR notes).

## Out of scope

- Full redesign of Actual's budget page (optional stretch only).
