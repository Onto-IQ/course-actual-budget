---
title: "[Stretch] Bot exchange-rate helper stub"
labels: stretch, api, course-stub
lab: stretch
---

## Task

Implement `course/src/fxRate.ts` with `convertTHB(amountThb: number, rate: number): number` and tests. Live HTTP call is optional; default to injected rate for tests.

## Acceptance criteria

- [ ] Tests green with injected rate.
- [ ] If HTTP is added, API key/token only via env.
