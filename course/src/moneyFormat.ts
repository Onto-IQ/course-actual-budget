/**
 * Course stubs — intentionally incomplete.
 * Learners implement these against failing tests (see course/tests).
 */

export function formatTHB(_amountBaht: number): string {
  throw new Error('Not implemented — Lab 02');
}

export function parseLooseAmount(_input: string): number | null {
  throw new Error('Not implemented — Lab 02');
}
